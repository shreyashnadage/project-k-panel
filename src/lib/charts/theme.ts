import { munimco } from "@/lib/brand/tokens";

export const chartColors = [
  munimco.navy,
  munimco.coTeal,
  munimco.amber,
  munimco.saffron,
  munimco.textSecondary,
];

export const chartGrid = { stroke: munimco.borderLight, strokeDasharray: "3 3" };
export const chartGridDark = { stroke: munimco.borderDark, strokeDasharray: "3 3" };
export const chartAxis = { fill: munimco.textSecondary, fontSize: 12 };
export const chartAxisDark = { fill: munimco.textSecondaryDark, fontSize: 12 };
